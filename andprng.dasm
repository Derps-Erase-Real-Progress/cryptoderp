;PRNG
;using AND

SET A, 0x1245
SET B, 0xCDFA
;SET C, 0x81E9

SET X, 0x8000
SET Y, 512
ADD Y, X

:loop

MUL A, A
XOR A, O

SET [X], A
ADD X, 1
IFG Y, X
SET PC, loop
:EOF SET PC, EOF

;
SET I, A

AND A, B
AND B, C
AND C, I

BOR A, B
BOR A, C



;MUL X, Y
;ADD X, O
;SUB Y, X
;ADD Y, O
